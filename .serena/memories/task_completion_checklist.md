After completing code changes in this project, normally do:
1) Run formatter: ./vendor/bin/pint
2) Run tests: php artisan test
3) If frontend assets changed, run npm run build (or npm run dev during development)
4) Review git diff for unintended changes
5) Ensure changes follow Laravel structure/conventions and .editorconfig rules
